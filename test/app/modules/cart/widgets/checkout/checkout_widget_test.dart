

void main() {
//  testWidgets('CheckoutWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(CheckoutWidget()));
//    final textFinder = find.text('Checkout');
//    expect(textFinder, findsOneWidget);
//  });
}
