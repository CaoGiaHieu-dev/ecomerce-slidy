import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('CategoryPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CategoryPage(title: 'Category')));
    //  final titleFinder = find.text('Category');
    //  expect(titleFinder, findsOneWidget);
  });
}
