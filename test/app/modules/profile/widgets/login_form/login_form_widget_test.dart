

void main() {
//  testWidgets('LoginFormWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(LoginFormWidget()));
//    final textFinder = find.text('LoginForm');
//    expect(textFinder, findsOneWidget);
//  });
}
