import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('HeaderPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(HeaderPage(title: 'Header')));
    //  final titleFinder = find.text('Header');
    //  expect(titleFinder, findsOneWidget);
  });
}
