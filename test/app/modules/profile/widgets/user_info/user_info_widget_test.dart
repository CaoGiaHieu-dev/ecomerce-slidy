

void main() {
//  testWidgets('UserInfoWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(UserInfoWidget()));
//    final textFinder = find.text('UserInfo');
//    expect(textFinder, findsOneWidget);
//  });
}
