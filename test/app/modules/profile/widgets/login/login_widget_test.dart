

void main() {
//  testWidgets('LoginWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(LoginWidget()));
//    final textFinder = find.text('Login');
//    expect(textFinder, findsOneWidget);
//  });
}
