import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ecomerce/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // CategoryController category;
  //
  setUp(() {
    //     category = HomeModule.to.get<CategoryController>();
  });

  group('CategoryController Test', () {
    //   test("First Test", () {
    //     expect(category, isInstanceOf<CategoryController>());
    //   });

    //   test("Set Value", () {
    //     expect(category.value, equals(0));
    //     category.increment();
    //     expect(category.value, equals(1));
    //   });
  });
}
